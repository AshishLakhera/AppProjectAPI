using System;

namespace Benchmarking.Model
{
    public enum ConnectionStringsCollection
    {
        DatabaseConnection,
        DatabaseConnectionRMS
    }
    public enum Clients
    {
        CS,
        DB,
        EVS
    }
}
